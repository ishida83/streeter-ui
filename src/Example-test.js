import { expect } from 'chai';

describe('Example', () => {
  describe('addition', () => {
    it('success', () => {
      expect(1 + 2).to.equal(3);
    });
    it('hi', () => {
      expect(3).to.equal(3);
    });
  });
});
